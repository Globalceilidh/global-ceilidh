import TestSurface from '../_components/TestSurface'

// /AnTonn/ceol — Ceòl (Music). Promoted from /AnTonn/ceol/test (2026-07-28);
// the test route 301-redirects here. The music CHART (the ledger Top 10) gets
// wired in next; for now it carries the navy TestSurface scaffolding.

export const metadata = {
  title: 'Ceòl · An Tonn',
  description: 'Ceòl (Music) — the An Tonn music current.',
}

export default function Ceol() {
  return <TestSurface background="#0B1739" waveMod="#3A6ACF" />
}
